spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/spring_crud
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernamte.dialect: org.hibernate.dialect.MySQL5Dialect